# CLAUDE.md

This project's agent guidance lives in **[AGENTS.md](AGENTS.md)** — read it first.

Quick reference: `npm install` → `npm run dev`. Run `npm run build` (which type-checks) before finishing any change. Content is data-driven from `src/cards.ts` + `src/models.ts`. No assets, no backend, no env vars. For copy-paste feature prompts see [PROMPTS.md](PROMPTS.md).
